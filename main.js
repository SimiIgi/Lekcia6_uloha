

/* const parseDate = (datum) => {
    return {
        day: Number(datum.slice(0, 2)),
        month: Number(datum.slice(3, 5)),
        year: Number(datum.slice(6))
    }
}

parseDate("01.01.2023") */




/* const date = ({ day: 6, month: 4, year: 2021 })

const formatDate = (date) => {
    let day = date.day
    let month = date.month
    let year = date.year

    return day + "." + month + "." + "year"

}



let myDate = 
 */
/* formatDate({ day: 6, month: 4, year: 2021 }) */



/* const round = (myNumber) => {
    const a = Math.trunc(myNumber)
    const b = myNumber - a
    if (Math.abs(b) === 0.5) {
        if (a % 2 === 0) {
            return a
        } else {
            return a + 2 * b
        }
    }
    return Math.round(myNumber)
} */